package com.example.sensorListerners;
/*
import com.example.final_project.WifiConnection;

import android.hardware.Sensor;
import android.hardware.SensorEvent;
import android.hardware.SensorEventListener;
import android.hardware.SensorManager;

public class TopRotationListener implements SensorEventListener {

	WifiConnection client;

	public TopRotationListener() {
		client = WifiConnection.getInstance();
	}

	@Override
	public void onAccuracyChanged(Sensor sensor, int accuracy) {
	}

	@Override
	public void onSensorChanged(SensorEvent event) {
		switch (event.sensor.getType()) {
		case SensorManager.SENSOR_ACCELEROMETER:
			move(event);
			break;

		default:
			break;
		}

	}

	long time = 0, lastUpdatedTime = 0;

	private void move(SensorEvent event) {

		time = event.timestamp;
		
		if (time - lastUpdatedTime > 0) {
           
			
			
		}

	}

}*/
